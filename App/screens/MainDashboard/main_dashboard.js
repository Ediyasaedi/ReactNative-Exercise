import React from 'react';
import { View, Text, SafeAreaView, Button } from "react-native";
import { MainHeader } from '../../components';

export default MainDashboard = ({ navigation }) => {

    return (
        <View>
            <MainHeader navigation={navigation} />
        </View>
    )
}